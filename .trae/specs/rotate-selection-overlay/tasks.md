# Tasks
- [x] Task 1: 선택 오버레이를 도형 회전에 맞춰 회전 렌더링
  - [x] 오버레이(하이라이트+핸들)를 “도형 로컬 좌표(0,0 ~ size)” 기준으로 구성한다.
  - [x] `rotationRadians`를 기준으로 도형 중심에서 `Transform.rotate`(또는 동등한 방식)로 오버레이 전체를 회전시킨다.
  - [x] 회전 핸들의 상대 위치(상단 중앙 오프셋)도 회전 그룹 안에서 유지되도록 정리한다.

- [x] Task 2: 회전 좌표계에서 리사이즈 입력 보정
  - [x] 리사이즈 핸들의 드래그 델타를 “도형 로컬 축”으로 변환해 기존 리사이즈 규칙에 적용한다(예: 화면 델타를 -angle로 역회전).
  - [x] 최소 크기 클램프 및 링크 갱신 동작을 유지한다.

- [x] Task 3: 회전 좌표계에서 회전 입력 일관성 점검
  - [x] 회전 핸들 드래그 시 도형 중심 기준 각도 계산이 회전된 상태에서도 자연스럽게 동작하는지 확인/조정한다.
  - [x] FlexiPointer 기반 포인터 처리와 “핸들 외 영역 비간섭”이 유지되는지 확인한다.

- [x] Task 4: 수동 검증 및 빌드 검증
  - [x] 수동 검증(웹): 도형 회전 후 선택 테두리/핸들이 함께 회전하는지 확인한다.
  - [x] 수동 검증(웹): 회전된 상태에서 리사이즈/추가 회전이 가능한지 확인한다.
  - [x] `flutter analyze`, `flutter build web`를 통과한다.

# Task Dependencies
- Task 2 depends on Task 1
- Task 3 depends on Task 1
- Task 4 depends on Task 1, Task 2, Task 3
