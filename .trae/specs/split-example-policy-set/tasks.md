# Tasks
- [x] Task 1: ExamplePolicySet 관련 코드를 역할별 파일로 분리
  - [x] `ExamplePolicySet` 본체(도형 렌더링/정책)와 선택 오버레이 UI를 분리한다.
  - [x] `_ResizeHandle`, `_RotateHandle` 및 수학/좌표 유틸을 별도 파일로 이동한다.
  - [x] import/export를 정리해 기존 사용처가 변경 없이 동작하게 한다.

- [x] Task 2: 동작/빌드 검증
  - [x] `flutter analyze`, `flutter build web`를 통과한다.

# Task Dependencies
- Task 2 depends on Task 1
