name: flexi_editor
description: "Flexible Editor for Flutter"
version: 0.0.1
homepage: https://github.com/scadasystems
repository: https://github.com/scadasystems/flexi_editor

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  provider: ^6.1.2
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  
flutter: